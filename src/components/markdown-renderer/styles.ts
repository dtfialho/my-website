'use client'
import styled from 'styled-components'

export const ImageWrapper = styled.div`
  width: fit-content;
  margin: 0 auto 24px;
`

export const Text = styled.p`
  font-size: 18px;
  line-height: 1.5;
`

export const TextWithHighlight = styled.span`
  background-color: #f0f0f0;
  padding: 2px 4px;
  border-radius: 4px;
`
